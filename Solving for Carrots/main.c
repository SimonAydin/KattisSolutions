#include <stdio.h>

int main()
{
    int n, p;
    scanf("%d %d", &n, &p);
    for (int i = 0; i < n; i++)
    {
        char s[90];
        scanf("%s", s);
    }
    printf("%d", p);
    return 0;
}